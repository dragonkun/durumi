=render :partial => 'services'

- @service = Service.find_by_identifier("lastfm_recentlisten")
%h1 Last.fm service

-form_for(@feed, :url => admin_feeds_url, :action => :post) do |f|
  = f.error_messages
  %p
    %img{:src => "/images/lastfm.png", :alt => "me2day"}
    = "Last.fm username"
    %br/
    = hidden_field_tag 'feed[service_id]', @service.id
    = text_field :site, :parameter, { :class => 'text' }
  %p
    = f.submit "Create"
