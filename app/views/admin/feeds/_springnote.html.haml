=render :partial => 'services'

- @service = Service.find_by_identifier("springnote")
%h1 springnote service

-form_for(@feed, :url => admin_feeds_url, :action => :post) do |f|
  = f.error_messages
  %p
    %img{:src => "/images/springnote.png", :alt => "springnote"}
    = "Springnote username"
    %br/
    = hidden_field_tag 'feed[service_id]', @service.id
    = text_field :site, :parameter, { :class => 'text' }
  %p
    = f.submit "Create"
